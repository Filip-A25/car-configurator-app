export enum CarPosition {
  front = "front",
  back = "left",
  side = "side",
  backLeft = "back_left",
  frontLeft = "front-left",
}

export interface CarModelImages {
  front: string;
  back: string;
  side: string;
  back_left: string;
  front_left: string;
}
